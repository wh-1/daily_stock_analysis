name: Cron Test 2

on:
  schedule:
    - cron: '*/10 * * * *'   # 临时：每10分钟一次（测试调度器登记；确认后立即删除本文件）
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Echo trigger time
        run: echo "cron-test2 fired at $(date -u '+%Y-%m-%d %H:%M:%S UTC') | event=${{ github.event_name }} | schedule=${{ github.event.schedule }}"
